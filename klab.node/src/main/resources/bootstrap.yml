spring:
  profiles:
    default: true
  application:
    name: node-service
  cloud:
    bus:
      enabled: false
    discovery:
      enabled: false
    consul:
      enabled: false
      config:
        enabled: false
    kubernetes:
      enabled: false
      discovery:
        enabled: false
      config:
        enabled: false