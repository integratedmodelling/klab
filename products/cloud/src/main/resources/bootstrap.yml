spring:
  application:
    name: ${ENGINE_SERVICE:engine-service}
  cloud:
    consul:
      enabled: false
        
stats:
  server:
    url: "http://localhost:8083"

engine:
  agent:
    port: 8999
  session:
    inactive: 60
    
server: 
  port: 8283
  servlet: 
    context-path: /modeler